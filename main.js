/*
1. whats the output
console.log(2 + '2'); // ans: 22,  + symbol is used for addition for nums and concotinate 2 string
console.log(2 - '2'); //ans: 0, - is only used for nums substractions

*/

/**
 * 2. Remove duplicate value in a array
 * 
 * let nums = [1, 2, 2, 4, 5, 3]
 * console.log([...new set(nums)])
 * 
 */

 let func = function(){
   {
     let l = 'let' // let is a block as blocked scope
     var v = 'var' // var has a function scope 
   }
   console.log(l);
   console.log(v);
 }

 func()


